import React from 'react';
import { registerRootComponent } from 'expo';
 // Import the main component for the app
// Make sure the file exists as App.tsx or App.js in the correct directory
import App from '../../App';
// Register the main component as the root component of the app